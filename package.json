{
  "private": true,
  "workspaces": [
    "packages/cli"
  ],
  "devDependencies": {
    "husky": "^6.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "docs": "pipelinedoc generate --generatorFormat markdown fixtures/**/*.yml --out-dir fixtures/docs && pipelinedoc generate --generatorFormat asciidoc fixtures/**/*.yml --out-dir fixtures/docs",
    "docs:watch": "yarn docs --watch",
    "pre-commit": "yarn docs --assert-unstaged"
  }
}
